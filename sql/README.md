# Legacy SQL folder (do not use for new migrations)

Canonical migrations live in:

**`supabase/migrations/`**

Existing files in this folder are kept only for history and must **not** be edited.
Add every new schema change as the next numbered file under `supabase/migrations/`.
